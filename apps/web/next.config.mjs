/** @type {import('next').NextConfig} */
const nextConfig = {
  //   experimental: {
  //     instrumentationHook: true,
  //   },
  images: {
    domains: ["png.pngtree.com"],
  },
};

export default nextConfig;
